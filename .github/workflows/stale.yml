# SPDX-FileCopyrightText: Copyright (c) 2025 Broadsage <opensource@broadsage.com>
#
# SPDX-License-Identifier: Apache-2.0

---
name: "[Support] Issue and PR Lifecycle Management"

on:
  schedule:
    # Run daily at 02:00 UTC for stale management
    - cron: "0 2 * * *"
    # Run hourly for solved issue cleanup
    - cron: "0 * * * *"
  workflow_dispatch:
    inputs:
      action_type:
        description: "Type of action to run"
        required: false
        default: "both"
        type: choice
        options:
          - "stale"
          - "solved"
          - "both"

# Use restrictive default permissions
permissions:
  contents: read

jobs:
  # Handle solved issues - close them immediately
  close-solved-issues:
    runs-on: ubuntu-latest
    name: Close solved issues
    if: |
      (github.event.schedule == '0 * * * *' || 
       github.event.inputs.action_type == 'solved' || 
       github.event.inputs.action_type == 'both') &&
      github.repository_owner == 'broadsage'
    # Job-level permissions for closing solved issues
    permissions:
      issues: write
      pull-requests: write
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76 # v2.14.0
        with:
          egress-policy: audit

      - name: Close solved issues immediately
        uses: actions/stale@3a9db7e6a41a89f618792c92c0e97cc736e1b13f # v10.0.0
        with:
          repo-token: ${{ secrets.AUTOBOT_GITHUB_TOKEN || secrets.GITHUB_TOKEN }}
          any-of-labels: "solved"
          stale-issue-label: "solved"
          days-before-stale: 0
          days-before-close: 0
          stale-issue-message: ""
          close-issue-message: |
            This issue has been automatically closed because it was marked as solved.

            Thank you for your contribution! 🙏

  # Handle stale issues and PRs
  stale:
    runs-on: ubuntu-latest
    name: Manage stale issues and PRs
    if: |
      github.event.schedule == '0 2 * * *' || 
      github.event.inputs.action_type == 'stale' || 
      github.event.inputs.action_type == 'both' ||
      github.event_name == 'workflow_dispatch'
    # Job-level permissions for stale operations
    permissions:
      issues: write
      pull-requests: write
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@20cf305ff2072d973412fa9b1e3a4f227bda3c76 # v2.14.0
        with:
          egress-policy: audit

      - uses: actions/stale@3a9db7e6a41a89f618792c92c0e97cc736e1b13f # v10.0.0
        with:
          # Repository token
          repo-token: ${{ secrets.GITHUB_TOKEN }}

          # Issues configuration
          stale-issue-message: |
            This issue has been automatically marked as stale because it has not had recent activity. 
            It will be closed in 7 days if no further activity occurs. 

            If you believe this issue is still relevant, please:
            - Comment on this issue to keep it open
            - Provide additional context or updates
            - Submit a pull request that addresses the issue

            Thank you for your contributions to this project! 🙏

          close-issue-message: |
            This issue has been automatically closed due to inactivity.

            If you believe this issue should remain open, please reopen it with additional context or updates.
            Thank you for your understanding! 🙏

          # Pull request configuration
          stale-pr-message: |
            This pull request has been automatically marked as stale because it has not had recent activity.
            It will be closed in 7 days if no further activity occurs.

            If this PR is still relevant, please:
            - Rebase your branch on the latest main/develop branch
            - Address any merge conflicts
            - Respond to any review comments
            - Add a comment explaining the current status

            Thank you for your contribution! 🙏

          close-pr-message: |
            This pull request has been automatically closed due to inactivity.

            If you'd like to continue with this contribution:
            - Reopen this PR and address the feedback
            - Create a new PR with the updated changes

            We appreciate your contribution and look forward to your continued involvement! 🙏

          # Timing configuration (in days)
          days-before-stale: 60 # Mark as stale after 60 days of inactivity
          days-before-close: 7 # Close after 7 additional days
          days-before-pr-close: 7 # Close PRs after 7 days of being stale

          # Labels configuration
          stale-issue-label: "stale"
          stale-pr-label: "stale"
          exempt-issue-labels: "pinned,security,enhancement,good first issue,help wanted,priority-high,priority-critical"
          exempt-pr-labels: "work-in-progress,security,priority-high,priority-critical,dependencies"
          exempt-draft-pr: true

          # Advanced configuration
          operations-per-run: 100 # Process up to 100 items per run
          remove-stale-when-updated: true # Remove stale label when updated
          enable-statistics: true # Show statistics in logs

          # Exemptions for specific cases
          exempt-all-pr-assignees: true # Don't mark PRs with assignees as stale
          exempt-all-issue-assignees: true # Don't mark issues with assignees as stale

          # Only process items without recent activity
          ignore-updates: true # Only consider comments, not all updates

          # Rate limiting to be respectful to GitHub API
          ascending: true # Process oldest items first
